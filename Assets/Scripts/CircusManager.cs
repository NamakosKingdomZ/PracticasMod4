using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CircusManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        AlienPerformer alien = new AlienPerformer("Malabares de Spectrums", "Golpes de maiz");
        Planet planet = new Planet("Vecturs V", 2.5f, false);
    
    }   

  
}
